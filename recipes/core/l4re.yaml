inherit: [ make ]

depends:
    - tools:
        target-toolchain: host-compat-toolchain
      depends:
        - name: devel::dtc
          use: [tools]
        - name: bsp::uboot-tools
          use: [tools]

checkoutSCM:
  - scm: git
    url: https://github.com/l4re/mk.git
    branch: main
    dir: .
  - scm: git
    url: https://github.com/l4re/acpica.git
    branch: main
    dir: pkg/acpica
  - scm: git
    url: https://github.com/l4re/ahci-driver.git
    branch: main
    dir: pkg/ahci-driver
  - scm: git
    url: https://github.com/l4re/bootstrap.git
    branch: main
    dir: pkg/bootstrap
  - scm: git
    url: https://github.com/l4re/cons.git
    branch: main
    dir: pkg/cons
  - scm: git
    url: https://github.com/l4re/drivers-frst.git
    branch: main
    dir: pkg/drivers-frst
  - scm: git
    url: https://github.com/l4re/gnu-efi.git
    branch: main
    dir: pkg/gnu-efi
  - scm: git
    url: https://github.com/l4re/hello.git
    branch: main
    dir: pkg/hello
  - scm: git
    url: https://github.com/l4re/io.git
    branch: main
    dir: pkg/io
  - scm: git
    url: https://github.com/l4re/ipcbench.git
    branch: main
    dir: pkg/ipcbench
  - scm: git
    url: https://github.com/l4re/l4_virtio-net-switch.git
    branch: main
    dir: pkg/virtio-net-switch
  - scm: git
    url: https://github.com/l4re/l4re-core.git
    branch: main
    dir: pkg/l4re-core
  - scm: git
    url: https://github.com/l4re/l4virtio.git
    branch: main
    dir: pkg/l4virtio
  - scm: git
    url: https://github.com/l4re/libblock-device.git
    branch: main
    dir: pkg/libblock-device
  - scm: git
    url: https://github.com/l4re/libfdt.git
    branch: main
    dir: pkg/libfdt
  - scm: git
    url: https://github.com/l4re/libvcpu.git
    branch: main
    dir: pkg/libvcpu
  - scm: git
    url: https://github.com/l4re/nvme-driver.git
    branch: main
    dir: pkg/nvme-driver
  - scm: git
    url: https://github.com/l4re/rtc.git
    branch: main
    dir: pkg/rtc
  - scm: git
    url: https://github.com/l4re/tinit.git
    branch: main
    dir: pkg/tinit
  - scm: git
    url: https://github.com/l4re/tvmm.git
    branch: main
    dir: pkg/tvmm
  - scm: git
    url: https://github.com/l4re/uvmm.git
    branch: main
    dir: pkg/uvmm
  - scm: git
    url: https://github.com/l4re/zlib.git
    branch: main
    dir: pkg/zlib

buildTools: [host-toolchain, target-toolchain, flex, bison]
buildToolsWeak: [dtc, uboot-tools]
buildVars: [ARCH, CROSS_COMPILE, L4RE_DEFCONFIG, L4RE_KCONFIG, L4RE_TARGETS,
            L4RE_MAKECONF]
buildScript: |
  if [[ ! -e build/.bob-init-done ]] ; then
    rm -rf build
    if [[ -n ${L4RE_KCONFIG+true} ]] ; then
      if [[ -n ${L4RE_DEFCONFIG+true} ]] ; then
        cp "$1/mk/defconfig/config.$L4RE_DEFCONFIG" defconfig
      else
        rm -f defconfig
      fi
      printf "$L4RE_KCONFIG" >> defconfig
      make -C "$1" B="$PWD/build" DEFCONFIG="$PWD/defconfig"
      make -C build olddefconfig
    elif [[ -n ${L4RE_DEFCONFIG+true} ]] ; then
      make -C "$1" B="$PWD/build" DEFCONFIG="$1/mk/defconfig/config.$L4RE_DEFCONFIG"
    else
      echo "Error: neither L4RE_DEFCONFIG nor L4RE_KCONFIG set!" >&2
      exit 1
    fi
    touch build/.bob-init-done
  fi

  if [[ -n ${L4RE_MAKECONF+true} && ! -e build/conf/Makeconf.local ]] ; then
    mkdir -p build/conf
    printf "$L4RE_MAKECONF" >> build/conf/Makeconf.local
  fi

  cat >modules.list <<EOF
  entry[no-defaults] dummy
  bootstrap bootstrap -modaddr 0x01100000
  EOF

  mkdir -p images
  case $ARCH in
    arm64)
      if grep -q sbsa build/.config.platform ; then
        IMAGES=( efi )
        EFI_NAME=bootaa64.efi
      else
        IMAGES=( elf raw uimage )
      fi
      ;;
    x86_64)
      IMAGES=( elf efi )
      EFI_NAME=bootx64.efi
      ;;
    *)
      IMAGES=( elf raw uimage )
      ;;
  esac

  pushd build
  make check_and_adjust_ram_base
  makeParallel ${L4RE_TARGETS:-}
  for i in "${IMAGES[@]}" ; do
    case $i in
      efi)
        # Workaround for broken bootstrap EFI incremental build...
        rm -f bin/*/plain/bootstrap.efi
        make efiimage E=dummy MODULES_LIST="$BOB_CWD/modules.list"
        IMG_NAME="$EFI_NAME"
        ;;
      elf)
        make elfimage E=dummy MODULES_LIST="$BOB_CWD/modules.list"
        IMG_NAME="bootstrap.elf"
        ;;
      raw)
        make rawimage E=dummy MODULES_LIST="$BOB_CWD/modules.list"
        IMG_NAME="bootstrap.raw"
        ;;
      uimage)
        make uimage E=dummy MODULES_LIST="$BOB_CWD/modules.list"
        IMG_NAME="bootstrap.uimage"
        ;;
    esac
    cp images/bootstrap_dummy.$i ../images/$IMG_NAME
  done
  popd

packageScript: |
  rsync -a --copy-unsafe-links \
    --include="/assets/***" \
    --include=/bin/ \
    --exclude=/bin/host/ \
    --include="/bin/*/" \
    --include="/bin/*/l4f/***" \
    --include=/lib/ \
    --exclude=/lib/host/ \
    --include="/lib/*/" \
    --include="/lib/*/std/" \
    --include="/lib/*/std/l4f/" \
    --include="/lib/*/std/l4f/*.so" \
    --include="/lib/*/std/l4f/.debug/***" \
    --exclude="*" \
    "$1/build/" .
  cp -a "$1/images/" .
